{
  "category": "{{item.category}}",
  "categoryName": "{{item.category-name}}",
  "title": "{{ item.title | upcase }}",
  "order": {{item.order}},
  "number": {{item.number}},
  "content": {{item.content | markdownify | json }}
}